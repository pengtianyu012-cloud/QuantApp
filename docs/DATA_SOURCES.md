# 数据源说明

当前阶段尚未接入真实行情数据源。

后续实现顺序：

1. Mock 数据源：用于单元测试、UI联调和无网络演示。
2. 本地 CSV/Parquet 数据源：用于可重复回测。
3. 免费行情适配器：接入前必须检查官方文档和实际可用性。
4. 付费数据源扩展：仅提供配置入口，不提交密钥。

数据源必须具备：超时、有限重试、指数退避、限流、本地缓存、字段校验和异常降级。

如果字段不存在，界面必须显示“数据源不支持”，不得伪造数值。
